{
  "name": "next-hono-firebase-starter",
  "private": true,
  "packageManager": "pnpm@9",
  "engines": {
    "node": ">=18.18"
  },
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "seed": "tsx tools/scripts/seed.ts"
  },
  "devDependencies": {
    "turbo": "^2.0.5",
    "tsx": "^4.15.7",
    "typescript": "^5.5.4"
  }
}
